# 复制为 .dev.vars 并填入本地开发值(合成值即可);staging/生产用 wrangler secret put。
# BETTER_AUTH_SECRETS 形态:<version>:<secret>(Better Auth 轮换格式)
BETTER_AUTH_SECRETS=1:change-me-local-only
GITHUB_CLIENT_ID=change-me
GITHUB_CLIENT_SECRET=change-me
